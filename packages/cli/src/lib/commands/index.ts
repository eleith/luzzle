import edit from './edit'
import editConfig from './editConfig'
import attach from './attach'
import process from './process'

export type { Command, Context } from './_types'

export default { edit, editConfig, attach, process }
