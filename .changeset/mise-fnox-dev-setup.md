---
"@luzzle/web": patch
---

Manage local dev secrets and docker compose tasks with mise + fnox instead of a plain `.env` file, so dev credentials have safe placeholder defaults committed alongside the config.
