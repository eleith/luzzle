---
"@luzzle/web": patch
---

Replace the hardcoded ▼ arrow in the `Combobox` component and the search dialog's type `Select` with a shared `ph:caret-up-down` icon, and fix the combobox trigger button only showing the currently selected item instead of the full list.
