---
"@luzzle/web": patch
---

Replace the directory and type dropdowns on the piece create form with a searchable combobox (new shared `Combobox` component built on bits-ui), so directories and types can be filtered by typing instead of scrolling a plain select.
