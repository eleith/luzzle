workspace:
  base: /workspace
  path: app

when:
  - event: push
    branch: main
    path:
      include: [".woodpecker/.web-lsp.yml", "packages/web/lsp/**"]
  - event: tag
    ref: "refs/tags/web.lsp/v*.*.*"

steps:
  prepare:
    image: node:24
    commands:
      - printf '%s' "$LUZZLE_NPMRC" | sed 's/[[:space:]]*$//' >> .npmrc
    environment:
      LUZZLE_NPMRC:
        from_secret: luzzle_npmrc

  test:
    image: node:24
    commands:
      - npm ci -w @luzzle/web.lsp
      - npm run lint -w @luzzle/web.lsp
      - npm run test -w @luzzle/web.lsp

  check-version:
    image: node:24
    commands:
      - cd packages/web/lsp
      - |
        VERSION_FROM_PKG=$(npm pkg get version --workspaces=false)

        if [ "\"$VERSION_FROM_TAG\"" != "$VERSION_FROM_PKG" ]; then
          echo "Version mismatch: Git tag version "$VERSION_FROM_TAG" does not match package.json version: $VERSION_FROM_PKG"
          exit 1
        fi
    environment:
     VERSION_FROM_TAG: ${CI_COMMIT_TAG#web.lsp/v}
    when:
      - event: tag

  extract-semver-tags:
    image: alpine/git:latest
    commands:
      - |
        VERSION_FULL="${CI_COMMIT_TAG#web.lsp/v}"
        VERSION_MAJOR=$(echo "$VERSION_FULL" | cut -d. -f1)
        VERSION_MINOR=$(echo "$VERSION_FULL" | cut -d. -f2)

        SEMVER_FULL="$VERSION_FULL"
        SEMVER_MAJOR_MINOR="$VERSION_MAJOR.$VERSION_MINOR"
        SEMVER_MAJOR="$VERSION_MAJOR"

        echo "Extracted versions: $SEMVER_FULL, $SEMVER_MAJOR_MINOR, $SEMVER_MAJOR, "
        echo -e "$SEMVER_FULL\n$SEMVER_MAJOR\n$SEMVER_MAJOR_MINOR" >> .tags
        cat .tags
    when:
      - event: tag

  build-docker-main:
    image: woodpeckerci/plugin-docker-buildx:6.0.3
    settings:
      repo: git.eleith.com/eleith.private/luzzle-web-lsp
      username: eleith
      dockerfile: packages/web/lsp/Dockerfile
      registry: https://git.eleith.com
      secrets:
        - 'id=npmrc\,src=.npmrc'
      context: packages/web/lsp
      password:
        from_secret: gitea_docker_registry
      tags:
        - latest
    when:
      - event: push
        branch: main

  build-docker-tag:
    image: woodpeckerci/plugin-docker-buildx:6.0.3
    settings:
      repo: git.eleith.com/eleith.private/luzzle-web-lsp
      username: eleith
      dockerfile: packages/web/lsp/Dockerfile
      registry: https://git.eleith.com
      context: packages/web/lsp
      secrets:
        - 'id=npmrc\,src=.npmrc'
      password:
        from_secret: gitea_docker_registry
      tags_file: .tags
    when:
      - event: tag

  notify-success:
    image: deblan/woodpecker-email:3.2.0
    settings:
      dsn:
        from_secret: woodpecker_email_dsn
      from:
        address:
          from_secret: woodpecker_email_from_address
        name:
          from_secret: woodpecker_email_from_name
      recipients:
        from_secret: woodpecker_email_recipient
      level: success
      recipients_only: true
    when:
      - status: [success]
  notify-failure:
    image: deblan/woodpecker-email:3.2.0
    settings:
      dsn:
        from_secret: woodpecker_email_dsn
      from:
        address:
          from_secret: woodpecker_email_from_address
        name:
          from_secret: woodpecker_email_from_name
      recipients:
        from_secret: woodpecker_email_recipient
      level: failure
      recipients_only: true
    when:
      - status: [failure]
