import Link from 'next/link'

# Book Archive

to help me refer to books i've read, i track them in this archive

## Most recent

the last book i've read: <Link href={`books/${props.book1.slug}`}><a>{props.book1.title}</a></Link>

## Next most recent

the last book i've read before the last book i've read: <Link href={`books/${props.book2.slug}`}><a>{props.book2.title}</a></Link>

## More

all the [other books](/books) i've read (minus children books because my memory isn't that good)

